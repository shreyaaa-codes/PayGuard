{
  "name": "payguard-contracts",
  "version": "1.0.0",
  "description": "Solidity contracts for PayGuard",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.4",
    "ethers": "^5.7.2",
    "hardhat": "^2.17.0"
  }
}
