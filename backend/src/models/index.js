// Centralized model exports (optional convenience)
const Job = require('./Job');
const Submission = require('./Submission');
const Review = require('./Review');

module.exports = { Job, Submission, Review };
